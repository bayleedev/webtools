import React from 'react';
import { GoogleCalendarLink } from '../types';

export interface GoogleLinkProps {
  link: GoogleCalendarLink
}

export const GoogleLink = (props: GoogleLinkProps) => {
  return (
    <span>Not ready</span>
  );
}
